{
  "name": "rememly-workers",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply rememly-db --local",
    "db:migrate:staging": "wrangler d1 migrations apply rememly-db --env staging",
    "db:migrate:production": "wrangler d1 migrations apply rememly-db --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260303.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.6.0"
  }
}
